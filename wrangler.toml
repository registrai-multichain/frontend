# Cloudflare Pages deployment config for registrai.cc
# Deploy: `npm run deploy` (runs `next build` then publishes the `out/` dir)
name = "registrai"
compatibility_date = "2026-05-01"
pages_build_output_dir = "out"
